{
  "name": "skypulse-backend",
  "version": "1.0.0",
  "description": "SkyPulse secure backend proxy — keeps Groq API key server-side",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
