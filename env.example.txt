# ── SkyPulse Environment Variables ───────────────────────────────────────────
# Copy this file to .env and fill in your real values.
# NEVER commit .env to version control.

# Your Groq API key (get one free at https://console.groq.com)
GROQ_API_KEY=your_groq_api_key_here

# Port the backend listens on (default: 3001)
PORT=3001

# Restrict CORS to your frontend's origin in production
# Leave as * for local development with a file:// or live-server frontend
FRONTEND_ORIGIN=*
